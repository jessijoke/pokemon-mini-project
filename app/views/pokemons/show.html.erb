<div class="page_title">
    <h1 class="h1_title">
        <%= @pokemon.name %> <%= (link_to 'edit', edit_pokemon_path(@pokemon)) %>
    </h1>
</div>

<div class="page_content">

    <h1>Team: <%= @pokemon.team.name %> </h1>

    <img src="<%= @pokemon_image %>">

    <p>Name: <%= @pokemon_name %></p>
    <p>Type: <%= @pokemon_type %></p>
    <p>Ability: <%= @pokemon_ability %></p>

</div>
